D1 To-Do (Today)

 1. Create the folder — make a portfolio-tracker folder
 2. Create 3 files — app.py, README.md, requirements.txt (empty is fine for now)
 3. Run a minimal Flask app — write it in app.py (type it yourself, no copy-paste)

Import Flask → app = Flask(__name__)
One home route @app.route('/') that returns any text
app.run(debug=True)


 4. Check it runs — run python app.py, open localhost:5000, success if text shows up
 5. Fill in the README — paste in the English README I made earlier
 6. First commit — git init → commit name Portfolio_D1_Setup → day 1 on the streak
