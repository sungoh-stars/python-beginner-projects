class SolutionReview1:
    def reverseStringSlicing(self, s: List[str]) -> None:
        left = 0
        right = len(s) - 1

        while left < right:
            s[left], s[right] = s[right], s[left]

            left += 1
            right -= 1 문제 2번 class SolutionReview2:
    def containsDuplicate(self, nums: List[int]) -> bool:

        nums.sort()
        for i in range(len(nums) -1):
            if nums[i] == nums[i + 1]:
                return True
        return False 문제 3번class SolutionReview3:
    def twoSum(self, nums: List[int]) -> List[int]:

        for i in range(len(nums)):
            for j in range(i + 1, len(nums)):
                if nums[i] + nums[j] == 7:
                    return [i,j]
        return []

- SolutionReview1: Two Pointer array reversal (In-place, O(1) space)
- SolutionReview2: Duplicate check via sorting & linear scan (O(N log N))
- SolutionReview3: Two Sum brute force with index offset tracking
