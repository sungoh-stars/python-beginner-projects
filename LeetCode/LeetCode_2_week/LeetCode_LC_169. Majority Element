class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        total = {}
        for num in nums:
            if num in total:
                total[num] += 1
            else:
                total[num] = 1

        for num in total:
            if total[num] > len(nums) //2:
                return num
